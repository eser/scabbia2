charset utf-8;

location ~ /^([^/]*/)*(\..+|web.config|scabbia|composer.json|composer.lock|writable/?|writable/.+|src/?|src/.+|vendor/?|^vendor/.+)$ {
    return 403;
}

location / {
    if (!-e $request_filename){
        rewrite ^(.*)$ /index.php last;
        break;
    }
}
