sources:
    - "{basepath}/src/"

commands:
    generate:
        callback: "Scabbia\\Commands\\GenerateCommand::generate"
        config:
            annotations:
                test:     {format: yaml}
            methods:
                - "MyProject\\FrontModule\\Processors\\Test::method"
