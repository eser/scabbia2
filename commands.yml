sources:
    - "{basepath}/src/"

commands:
    generate:
        callback: "MyProject\\FrontModule\\Commands\\Generator::generate"
        config:
            annotations:
                test:     {format: yaml}
            methods:
                - "MyProject\\FrontModule\\Processors\\Test::method"
