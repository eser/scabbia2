sources:
    - "{basepath}/src/"

commands:
    generate:
        callback: "Scabbia\\Framework\\Commands\\GenerateCommand::generate"
        config:
            annotations:
                event:        {format: yaml}
                route:        {format: yaml}
                # testfixture:  {format: yaml}
            methods:
                - "Scabbia\\Events\\Generator::generate"
                - "Scabbia\\Router\\Generator::generate"

    tests:
        callback: "Scabbia\\Testing\\TestsCommand::tests"
        config:
            fixtures:
                - "Scabbia\\Tests\\Yaml\\ParserTest"
                - "Scabbia\\Tests\\Yaml\\InlineTest"
